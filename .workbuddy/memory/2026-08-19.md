
## 首页改版为在线搜题（2026-08-19）
- 完全重写 intro/index.html：教育风格搜题首页。大搜索框（登录后可搜）、题库精选随机抽题（答案折叠展示）、右上角登录/注册/找回、QQ频道卡片【学神助手】https://pd.qq.com/s/ga2bgmmf8 频道号 pd22434767、使用步骤、FAQ。
- 前端登录态复用 localStorage `xxt_user_token` / `xxt_user_info`（与 user.html 一致），Bearer 调接口。
- 后端 backend.py 新增：POST /api/web/search（需登录、仅题库直查 get_question_bank_match、不扣点数、每日30次 WEB_SEARCH_DAILY_LIMIT、IP 30次/分钟限流、计入 bank_call_logs，match_method 加 web_ 前缀）；GET /api/web/random-questions?count=N（免登录，封顶12）；GET /api/web/search-quota（查今日剩余）。
- database.py 新增 web_search_daily 表（username+search_date 唯一键计数）与 get_web_search_count / increment_web_search_count / get_random_bank_questions 方法。
- 注意：每次搜索（含未命中）都计 1 次；改动需部署到 43.138.147.202 并重启 xs-backend 才生效，新表在服务启动时自动创建。
