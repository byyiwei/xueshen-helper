# 2026-08-23 工作日志

## 1. 清理 E:\Code\XS 本地"备份"
- 扫描无传统备份文件（.bak/.old/~/-copy 等均为 0）。可清理项：3 个 `__pycache__`（11 个 .pyc，~1.66MB，编译缓存可重建）+ `.trae-html-share-packages/intro/index.html.zip`（~157KB，Trae 一次性导出副本）。
- 用户确认两者都删，已 `rm -rf` 完成。src/__pycache__ 里同时有 cpython-310/312/313 三套过期缓存。

## 2. 搜题匹配诊断与修复
- 现象：首页/脚本UI 搜"加强和促进海内外中华儿女大团结"搜不到或只出 1 条，但后台 search_question_bank（LIKE+全文）能捞出很多。
- 根因：线上 `/api/web/search` 走 `get_bank_match_all`，只用 `matching_key`/`match_stem` 归一化哈希**精确匹配**，要求题干逐字相等；后台用模糊匹配，故"后台有、搜题没有/少"。
- 修复：在 `get_bank_match_all` 哈希未命中时加**模糊关键词回退**，直接复用 `db.search_question_bank(keyword=q_norm, limit=20)`（与后台一致）。已 py_compile 通过。
- 路径隔离（重要）：答题/自动答题走 `get_question_bank_match`（931、6242 行），**未改动**；搜题走 `get_bank_match_all`（仅 6141 行 /api/web/search 调用）。两者独立，不影响答题扣点数逻辑。
- 调用方：intro/index.html(909)、脚本 xueshen-sc.js(2377) 都调 `/api/web/search`；脚本UI OnlineSearch 模板用 renderList 渲染全部 matches，前端支持多结果。
- 状态：修复在本地，**尚未部署到 43.138.147.202 / 重启 xs-backend**；若后台"很多">20 条可调高回退上限。

## 部署：搜题多结果回退修复 (2026-08-23 22:43)
- 服务器 43.138.147.202(=www.openget.cn)。SSH 用 C:/Users/HUAWEI/.ssh/id_ed25519 作 root；id_ed25519_xs 是给 github 的，别搞混。
- 部署目录 /www/wwwroot/xs.openget.cn；后端重启：/www/server/panel/pyenv/bin/supervisorctl -c /etc/supervisor/supervisord.conf restart xs-backend:xs-backend_00
- 本次仅上传 src/backend.py（get_bank_match_all 加模糊关键词回退+去重，解决"搜题只返回1条"）。database.py 与服务器一致(本地 diff 0)，未动。
- 坑1：服务器 src/ 有孤儿进程(之前手动 python3 run_backend.py 起的)，supervisor restart 时新进程抢不到端口→FATAL。须先 kill 孤儿再重启。
- 坑2：pkill -f "backend.py"/"run_backend.py" 会误杀含该字符串的 ssh 命令本身，禁止用 pkill -f 带这些关键词，改用精确 PID kill。
- 验证：supervisor RUNNING pid 291401，random-questions 返回真实数据，模糊回退代码已落盘(行1655/1659)。
- 脚本 xueshen-sc.js/xueshen-gf.js 本地已 bump @version 5.2.5→5.2.6 并加 v5.2.6 changelog(仅元数据，未动答题逻辑)。需 push 到 github(byyiwei/xueshen-helper) 才对用户生效。
- 服务器 src/ 堆了大量 backend.py.bak_*/database.py.bak_* 备份(用户说的"备份清不干净"真身)，待确认后清理。
