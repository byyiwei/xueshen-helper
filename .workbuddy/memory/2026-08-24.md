
## 部署用户新改动到服务端 (2026-08-24 17:53)
- 用户本地有新未提交改动，要求更新服务端。本次部署 server 端文件：src/backend.py + static/admin.html（scripts 走 GitHub 不走此服务器，本次不动）。
- backend.py 改动（均已审查，未碰答题核心 get_question_bank_match）：
  - build_ai_question_text/build_ai_messages 题型映射扩充数字编码 0-25 中文名
  - AI 调用 /tmp 临时文件改 tempfile.gettempdir()（跨平台）；跨提供商轮询打散 provider_round_robin_rest + 重试 3->5
  - 新增管理端接口 /admin/question-bank/retrain（按题目指纹+指定模型重算答案，admin 鉴权）
- admin.html 改动（304行，配套重训练 UI）。
- 部署：SSH id_ed25519 root@43.138.147.202；备份 backend.py/admin.html -> .bak_20260824_175325；scp 上传；supervisor restart（SUP="/www/server/panel/pyenv/bin/supervisorctl -c /etc/supervisor/supervisord.conf" restart xs-backend:xs-backend_00）。
- 验证：新进程 pid 450346（17:53:44）；random-questions 200、search-quota 401。provider_round_robin_rest 已定义(2056行)，py_compile 通过。
- 注意：这些改动仍未 git commit（工作区脏）；两个脚本 xueshen-sc/gf.js 也有3行未提交改动，需另推 GitHub 才对用户端生效。
