- **权限配置**：必须在 `app.json` 中声明 `scope.userLocation` (安卓蓝牙搜索需要) 和 `scope.bluetooth` (iOS 蓝牙权限) 权限。
- **环境要求**：安卓需开启 GPS 定位并授权微信；iOS 需授权微信蓝牙权限。
- **构建命令**：`npm run build` (生产环境) 或 `npm run dev` (开发环境) 使用 Rollup 编译 TypeScript 源码。