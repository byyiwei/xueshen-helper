- 采用“静默登录”策略，启动时优先从本地存储读取 openid，缺失时异步调用云函数获取并自动更新全局状态，避免阻塞首屏渲染。
- 系统配置加载具备降级机制，优先从 `systemConfig` 集合读取，失败或为空时自动回退至旧的 `system` 集合，确保配置可用性。
- 全局样式使用 CSS 变量（如 `--primary-color`）定义设计令牌，并通过 `.btn-primary` 等语义化类名封装常用 UI 组件样式。