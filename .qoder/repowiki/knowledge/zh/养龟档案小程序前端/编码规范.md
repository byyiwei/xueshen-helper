- 所有业务逻辑通过 `utils/api.js` 的 `callCloudFunction` 统一发起，禁止在页面中直接调用 `wx.cloud.callFunction`。
- 页面文件遵循标准四件套结构（.js, .json, .wxml, .wxss），并按功能模块或分包目录进行物理隔离。
- 全局样式变量与基础设计规范统一定义在 `styles/design.wxss` 中，供各页面引用以保持 UI 一致性。