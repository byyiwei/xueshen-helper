- 所有云函数入口均采用基于 event.action 的路由分发模式，通过 switch-case 结构将请求分发给内部的 async 业务函数。
- 统一使用 successResponse 和 errorResponse 工具函数封装返回结果，确保前端接收到的数据结构包含 success, data/message 字段。
- 在执行写操作（增删改）前，强制进行 openid 权限校验或文档所有权验证，防止越权访问。
- 数据库查询普遍采用分页机制（pageNum/pageSize），并在返回列表时通过 normalizeId 将 _id 映射为 id 以兼容前端习惯。