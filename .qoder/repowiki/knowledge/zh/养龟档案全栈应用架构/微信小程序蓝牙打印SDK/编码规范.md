- 所有异步 BLE 操作（如连接、写入、读取）均封装为返回 `Promise<LPA_Response<T>>` 的形式，统一错误码处理。
- 绘图接口（如 `drawText`, `drawBarcode`）采用毫米（mm）作为逻辑坐标单位，内部自动转换为 Canvas 像素坐标。
- 打印任务通过 `startJob` 创建上下文，绘制完成后调用 `commitJob` 提交，支持预览模式（生成 Base64 图片）与实打模式。
- 适配器层 (`BleAdapter`) 使用静态单例模式 (`getInstance`) 管理蓝牙连接状态，避免重复初始化导致的资源冲突。