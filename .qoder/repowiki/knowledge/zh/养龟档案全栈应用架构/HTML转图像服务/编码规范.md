- 所有异步资源获取（如浏览器实例、页面创建）均包裹在 try-finally 块中，确保失败时能正确关闭子资源防止泄漏。
- 配置项的环境变量映射遵循 H2I_ 前缀约定，并通过双下划线 __ 处理嵌套对象中的字面量下划线键名。
- HTTP 响应统一通过 `sendJSON` 辅助函数发送，强制设置 Content-Type 为 application/json 并手动计算 UTF-8 字节长度。