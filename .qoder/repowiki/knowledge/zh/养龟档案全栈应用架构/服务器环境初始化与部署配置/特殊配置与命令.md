- 必须以 root 权限执行：`sudo bash setup.sh`。
- 数据库初始化命令：`mysql -u turtle_user -p turtle_archive < database.sql`。
- Nginx 配置部署：将 `turtle-archive.nginx` 移至 `/etc/nginx/sites-available/` 并创建至 `sites-enabled` 的软链接后重启服务。