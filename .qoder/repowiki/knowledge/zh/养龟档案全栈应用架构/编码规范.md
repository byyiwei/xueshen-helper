- 云函数统一采用 `{ action, data }` 入口参数结构，并通过 `wrapAction` 等工具函数标准化返回 `{ success, data, message }` 格式。
- 前端 API 调用封装为单例 `APIManager`，所有业务请求均通过 `callCloudFunction` 方法发起，实现统一的错误处理与加载状态管理。
- 跨模块共享工具类（如 `securityChecker`, `utils`）在 `cloudfunctions/common` 中定义，并在各业务云函数中通过 `require` 引用以保持逻辑一致。